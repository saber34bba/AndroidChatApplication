<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mouboukr.sofianeaoufi.chat">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/kalemni_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".FirstActivity" />
        <activity android:name=".UserProfileActivity" />
        <activity android:name=".ShowProfileSelectedActivity" />
        <activity android:name=".ChatActivity" />
        <activity android:name=".AllUsersActivity" />
        <activity android:name=".SeachrForPeopleActivity" />
        <activity android:name=".FriendshipRequestsActivity"></activity>
    </application>

</manifest>